# MakeMKV ingest

Confirm `makemkvcon` is available and inspect the source/disc before ingest.
Write only to a distinct work path with sufficient space. Preserve the source
unchanged, capture MakeMKV output and resulting probes, and do not automatically
delete or archive source material. Disc keys, credentials, and private tokens
must never enter Git or workspace logs.

